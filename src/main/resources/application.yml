spring:
  application:
    name: thymeleaf-training
  datasource:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
    url: jdbc:postgresql://localhost:5432/kata
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update